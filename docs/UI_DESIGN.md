# Solo Agency Bridge + Local UI — Design Contract (U-spec v1)

Status: U0 approved by the operator (2026-07-19). **U1 shipped** (read-only UI + G1 tool CLI). **U2 shipped 2026-07-19** (interactive approvals + shortlist via `ui_inbox`, consumed by `crm_store.py ingest-ui`; playbook wiring in AUTOMATION_SCHEDULING/06/09/DESIGN §14–15 and content playbook 02). **G2+G3 shipped 2026-07-19**: ALL six Python tools now live inside the bridge binary (`tool crm-store|import-leads|render-report|provider|gmail|verify-email`) with golden cross-validation against the Python originals (crm-store: 32-step scenario, stdout+tree parity incl. `ingest-ui`; import-leads: byte-identical idempotency keys; render-report: byte-identical HTML + scrub-gate parity; provider: stub-server parity incl. the composed notify; gmail: classifier/presend/dry-run/quota parity; **live SMTP/IMAP VALIDATED 2026-07-19 on a real @gmail.com sendbox** — Go auth (SMTP 465 + hand-rolled IMAP 993), Python health on Go-written credentials, Go draft → Python approve → Go real send → Python quota parity → Go sync fetched + classified the arriving mail as campaign_reply/from_address and froze the sequence; Python re-sync on the advanced cursor saw 0 new. The retirement precondition (parity + one real run) was met and **the retirement sweep executed 2026-07-19 on the operator's order: every `.py` tool and the Python test suite are deleted; the bridge binary is the only implementation; the golden xval tests became pure-Go behavioral scenarios asserting the Python-verified outcomes; the deploy preflight runs `go test`; Stage-0 Binary-First is binary-only (no fallback)**. Same slice added the lead-data taxonomy (DESIGN §9.1b): `identities.seeds[]` content clues (reel/video/post/blog) as dedupe-eligible records, URL-shape classification on import incl. per-line .txt fragments and multi-column clue mapping, the seed → profile → email resolution ladder with `seed_unresolved`/`name_only_fragment` enrich reasons, and canonical write-back of found profiles via `channels_found.profiles`). Windows is unlocked (flock/LockFileEx replaces fcntl). Playbooks now carry the Binary-First Tool Invocation rule (Stage 0 both sides). **U3a shipped 2026-07-19** (CRM list columns + full contact profile page with the hooks timeline). **U3-campaigns + autostart shipped 2026-07-20**: campaigns list + detail/edit pages with the operator whitelist (`campaignUpdate` in the store, `tool crm-store campaign update` on the CLI, `POST /api/ui/{c}/campaign-update` on the UI — the second sanctioned direct write, see §6), code-enforced paused semantics (queue no-ops with `skipped.campaign_paused`, draft write refuses, send parks a transient `campaign_paused` blocker that keeps the draft approved), and boot autostart in the setup scripts (macOS LaunchAgent `com.solo-agency.collector.{insthash}` with KeepAlive-on-failure, Linux systemd user unit + best-effort lingering, Windows logon Scheduled Task wrapped in `cmd /c` for logging; `SOLO_AGENCY_NO_AUTOSTART=1` opts out, clean `/shutdown` exits 0 so supervisors do not fight upgrades). **UI redesign shipped 2026-07-20 (v1.6, presentation-only):** dark-only design system (`app.css` vendored in-binary at `/ui/assets/app.css`, served no-cache so upgrades restyle instantly; Pico stays as the form/reset layer, forced `data-theme=dark`) with a blue-black canvas, layered surfaces and ONE lime accent; sidebar shell (global nav + per-client section with a live pending-approvals badge) + topbar breadcrumb + SSE "live" dot; home = hero tagline + cross-client stat tiles (clients / active campaigns / pending drafts / sent today) + a rich grouped feature catalog (Content pipeline, Outreach + CRM, Runs itself) with vendored Tabler icons and web-UI vs agent-chat chips; client overview = clickable stat tiles + icon action cards; campaigns get quota progress bars, approvals get band-colored draft cards under the sticky batch toolbar, extension gets a numbered stepper, CRM/contact/status/sendboxes/reports/jobs/locked restyled to the same tokens. Zero behavior change: no route/API/form-field/id was touched, every test-asserted string preserved, SSE/footform semantics intact; em-dash-free UI copy. **Host-header UI auth shipped 2026-09-10 (OWNER DECISION):** `--ui-auth host` is now the default — GET routes (`/ui`, `/files`, `/events`, etc.) are gated by validating the request's Host header against the bridge's own loopback address (`127.0.0.1`/`localhost`/`[::1]`, on its own port) instead of the token cookie, so the dashboard opens with no "Locked" page and no `/ui/enter/<token>` visit in any local browser (Claude Code's side Browser pane, the human's own browser, `open`/`start`/`xdg-open`); writes (`POST /api/ui/...`) keep a same-origin check (`Origin` in the same loopback set, or absent with `Sec-Fetch-Site` absent/none/same-origin) in BOTH modes; the old cookie-and-token gate survives unchanged as an opt-in (`--ui-auth token` or `SOLO_UI_AUTH=token`). See §4. **English-only UI + multi-browser install shipped 2026-09-10 (OWNER DECISION, v1.7):** every human-facing string in `ui*.go`'s Go templates and inline JS (and the Chrome extension popup) is English now — see §1 principle 6; the extension page's install flow gained a browser + Chrome-profile picker backed by a new `ui_browsers.go` (`detectBrowsers` reading each Chromium-based browser's own "Local State" JSON, a new `GET /api/ui/{c}/browsers`, and an optional `{browser, profile_directory}` body on `POST /api/ui/{c}/install-extension` that opens the chosen browser/profile's extensions page and remembers the choice in `extension_registry.json`) — see the `/ui/{client_slug}/extension` and `/api/ui/{client_slug}/browsers` rows in §5. **Deep-linking shipped 2026-09-10 (OWNER DECISION, v1.9):** "phải update dashboard sao cho có thể mở mọi thứ bằng url" — every view, tab and filter is now reachable by a URL. `ui_routes.go` carries ONE Go table, `uiRoutes` — every `/ui/...` page, its query params (one-line meaning each) and what it shows — as the single source of truth; it is exposed three ways so an agent never has to memorize a path: `GET /api/ui/routes` (JSON, `uiAuth`-gated like other `/api/ui` reads), `GET /ui/routes` (the same table as a small English page), and `tool ui routes` (plain text, no bridge needed). `TestUIRoutesTableCoversRouterCases` parses `handleUIRouter`'s switch (the `// CaseKey: x` comment beside each case) and fails the build if a page is added to the router without a matching `uiRoutes` entry, or vice versa; `TestUIRoutesTableWalksAllPaths` GETs every table path with a dummy client/campaign/contact and asserts 200 (302 for `contact=`). Server-filtered pages (CRM, reports, campaigns, jobs, fleet, status, sources, shortlist) read `r.URL.Query()` and filter before paginating; client-only-filtered pages (approvals, sent, campaign-detail tabs, the extension page's browser/profile picker) read `location.search` in an inline script on load and call `history.replaceState` on every filter change, so the current view is always the URL in the address bar — see the param list per row below. Every page also carries stable `id` attributes on its major sections (`#stats`, `#pending`, `#features`, `#latest` on the client-overview page; `id="d-<date>"` per day on reports; `id="job-<id>"` per row on jobs; `id="fleet-<slug>"` per tile on fleet; `id="section-<x>"` on status and on the campaign-detail tabs) so a `?param=` or a bare `#fragment` scrolls straight to it. **`POST /api/ui/show`** (`{"url":"/ui/leadup/crm?locked=1"}`, same-origin-gated like every other `/api/ui` write) broadcasts an SSE `"show"` event (`uiShowHub`, the same broadcaster `/events` already runs) to every connected browser tab; the "foot" template's `EventSource` listener does `location.assign(url)` unless the page is dirty (unsaved input), in which case it shows the same sticky bar the "change" event uses, but as an "Open `<url>`" link instead of an auto-refresh button — this is how an agent points the operator's own already-open dashboard tab at the exact view it just described in chat, without a second click. `uiShowURLAllowed` accepts only a relative path starting `/ui/` or `/files/` — an absolute URL, a protocol-relative `//host`, `../` traversal, an encoded slash/backslash/NUL, a bare scheme, or a raw CR/LF (which would otherwise let a POST body inject extra fields into every connected browser's SSE stream) all 400. `tool ui show <path> [--bridge URL]` is the CLI twin — a thin POST client, for an agent runtime with no browser tab of its own to react to the SSE event but a human watching a UI a different way. This document is the authoritative contract for the localhost UI and for absorbing the Python tools into the single bridge binary. Implementation phases below reference it; when any implementation disagrees with this file, this file wins (amend it first, then the code). (v1.9)

## 1. Purpose and principles

Solo Agency is a new kind of software: an AI agent (any vendor — Claude, Codex, Hermes, OpenClaw, ...) is the orchestrator and tour guide, chat is the conversational spine, and a localhost web UI covers everything chat can never do well (tables, buttons, live status, batch review). Principles, in priority order:

1. **The filesystem is the bus.** Every agent's I/O is files under `daily-content-pipeline/`. No agent ever depends on HTTP to the bridge: agents in sandboxes often cannot reach localhost (see the Sandbox Localhost Rule). The UI and the agents meet only at files.
2. **Agents produce links, never fetch them.** An agent constructs UI URLs as static strings from the URL map below and hands them to the human. The human's browser (on the human's own machine) opens them. An agent must never HTTP-GET a UI URL, and a UI URL being unreachable from a sandbox proves nothing. **The SHOW RULE (OWNER DECISION 2026-09-10) — how the link actually gets in front of the human:** (1) always print the link as plain text in the reply, on every runtime — this is the floor; (2) on Claude Code desktop, also open it in the side Browser pane; (3) on any other local runtime, also shell out to the OS opener so it appears in a real browser window — `open <url>` (macOS), `start <url>` (Windows), `xdg-open <url>` (Linux). Never HTTP-GET a link to "verify" it — that is the fetch this principle already forbids, and a failed GET from a sandboxed agent process proves nothing about the human's own browser. This never applies inside a client-facing file (Client-Blind Deliverable And Internal Report Rule, `AGENTS.md`) — a dashboard/CRM/help link is agent-to-operator only. **Codex desktop is an equal of step (2):** it carries its own built-in browser pane beside the chat, so a Codex-runtime agent opens the link there exactly as a Claude Code agent opens it in the side Browser pane. For a local runtime with no browser pane of its own, step (3) is `tool ui show <path>` first — it moves an already-open dashboard tab to that URL over the `POST /api/ui/show` SSE `"show"` event (§4/v1.9) — falling back to the OS opener only when the bridge reports zero connected tabs. Which exact page to open is never memorized: the agent looks it up in the routes table (`tool ui routes` / `GET /api/ui/routes` / `/ui/routes`, this section's v1.9 note above) and matches the question to a route's "shows" line — the Answer-and-Show Rule (`SOLO_AGENCY_PLAYBOOK.md`, "Team Leader Reply Frame") is the full policy for when this firing is mandatory.
3. **One process.** The existing collector bridge (`solo-agency-collector/bridge-go`, `127.0.0.1:17321`, Go stdlib only, single binary) grows four roles; nothing else runs. No second daemon, no node server, no build chain.
4. **UI complements chat; chat stays sufficient.** Every UI surface has a chat fallback (today's flows). If the bridge is down, nothing is blocked — the UI is an enhancement, never a dependency. Natural-language, unstructured input stays in chat; structured review/action moves to UI.
5. **No new trust surface.** Nothing auto-sends. A button click is the same human approval as a chat `approve` — one more way for the human to decide, never a way for the system to decide.
6. **UI language: English only (OWNER DECISION 2026-09-10).** Every string a human sees in the bridge web UI — all Go `html/template` markup and inline JS in `ui*.go`, JS alerts, button labels, pills, diagnostics, error pages — and in the Chrome extension popup is English. Vietnamese belongs only in playbooks as tone examples and in chat, where the agent speaks the human's own language. A placeholder or helper string that must demonstrate a language-specific behavior (e.g. the content-search page's accent-insensitive matching) describes the behavior in English rather than embedding non-English example text. `ui_browsers_test.go`'s `TestUITemplatesEnglishOnly` enforces this by regexing the `uiTpl` template literal for Vietnamese-diacritic characters on every `go test`.

## 2. Where the UI fits the session model

The three session types are unchanged (see the OutreachCRM router "Session Model"): one shared setup session; one content automation session per client; one automation session per campaign. The UI is not a session — it is a window over the shared filesystem state that all sessions read/write. The chat agent guides the human into UI rooms via links; UI decisions flow back to sessions via files.

## 3. Process architecture — four roles in one binary

| Role | Description | Status |
|---|---|---|
| Extension hub | Existing 13 endpoints (`/status`, `/jobs/*`, `/collect/*`, `/complete`, ...), extension token auth. UNCHANGED; UI work must not alter these routes or their semantics. | shipped |
| Static/report server | Serve report HTML/PDF/assets from the data root read-only (`/files/...`), so handoffs become clickable URLs instead of file paths. | U1 |
| UI app | Embedded server-rendered pages + vanilla JS (Go templates compiled into the binary), served under `/ui/...`. No node, no build chain, no external CDN (self-contained like the reports). SSE-driven auto-refresh. (v1.1: server-rendered chosen over an SPA — same no-build guarantee, less client state. v1.3: styling is vendored Pico CSS v2 (MIT), embedded in-binary and served at `/ui/assets/pico.min.css` — a real design system with automatic dark mode, still zero CDN/build.) | U1–U3 |
| File-bus API + tools | Read APIs over existing files, SSE change feed, write endpoints restricted to `ui_inbox/`, and CLI subcommands replacing the Python tools (G1–G3). | U1–G3 |

## 4. Security

- Bind `127.0.0.1` only (already true). Never expose beyond localhost.
- **Auth mode — `--ui-auth host` (OWNER DECISION 2026-09-10, the default):** the bridge validates the incoming request's Host header against its own loopback address — exactly `127.0.0.1`, `localhost`, or `[::1]`, on the bridge's own port — and nothing else; no cookie, no token, no `/ui/enter/` visit required for `/ui`, `/files`, `/events`, or any other GET. This is enough to defeat DNS rebinding, the actual attack the token cookie existed for (a malicious page can trick the *browser* into believing it's talking to a domain that then resolves to `127.0.0.1`, but it cannot forge the Host header the browser sends — that still names the attacker's domain, which fails the check). A request whose Host header names anything else (a LAN IP, an evil domain) gets `421 Misdirected Request` with a one-line body, not the app. Why this is enough: unlike the token, it costs the human nothing — the dashboard just opens, in the Claude Code side Browser pane, `open`/`start`/`xdg-open`, or the human's own browser, with no "Locked" page ever shown.
- **Writes require same-origin, in BOTH modes.** Host-header validation alone does not stop a foreign page from POSTing straight at the bridge's real Host (the browser sets Host to match wherever the request is actually going, not the page's own origin) — so every state-changing route (`POST /api/ui/...` and any other write) additionally requires the `Origin` header's host to be in the same loopback allow-list, or `Origin` to be absent with `Sec-Fetch-Site` absent/`none`/`same-origin` (the signature of a curl call or a local agent process, which never sets browser fetch metadata at all). A foreign `Origin` or an explicit `Sec-Fetch-Site: cross-site` is rejected with `403` regardless of auth mode.
- **Legacy/opt-in — `--ui-auth token` (or `SOLO_UI_AUTH=token` when the flag itself is not passed):** the pre-2026-09-10 behavior, unchanged. On start the bridge writes `daily-content-pipeline/bridge/ui_token` (0600) containing a random token; the first browser open at `/ui/enter/{token}` sets a session cookie, and every route without it renders the "Locked" page. Re-enable this only when Host-header validation genuinely is not enough for an install's threat model — which in practice means the bridge is reachable from somewhere other than the operator's own loopback interface, already out of spec per the bullet above (never expose beyond localhost).
- The token file is still generated and still checked in host mode (harmless): `/ui/enter/{token}` keeps working in both modes, so a doc or agent that still hands out the old entry link never breaks — in host mode it just redirects to `/ui` without setting a cookie. The "Locked" page itself is shown only in token mode.
- The extension token mechanism is separate and unchanged.
- Everything the UI shows is operator-only by definition (it runs on the operator's machine). The client-blind rule still applies to anything exported OUT of the UI (the scrubbed weekly/monthly reports remain the only client-facing artifacts).

## 5. URL map (stable contract — agents build links from this table)

Base: `http://127.0.0.1:17321`

| Path | Surface | Phase |
|---|---|---|
| `/ui` | Home = Feature Catalog rendered clickable + client list + global status (v1.4: capability overview on home; per-client Action cards — `ui`-kind opens the page, `agent`-kind shows the exact FEATURE_CATALOG trigger phrase + copy button + which session to paste it into) | U1 |
| `/ui/settings` | Operator-wide system settings (concurrency, quiet hours, scheduled task slots). No params | U1 |
| `/ui/jobs` | Job queue & run monitor (collector jobs, automation runs, live states). `?client=<slug>` narrows the table to that client; `?job=<id>` highlights and scrolls to that run's row (`id="job-<id>"`) | U1 |
| `/ui/status` | Bridge / extensions / sendboxes / provider health. `?section=<id>` scrolls to one section (`bridge`\|`extensions`\|`healthcheck`\|`sendboxes`) | U1 |
| `/ui/fleet` | Every client's posting cadence + performance, worst-first. `?client=<slug>` highlights and scrolls to that client's tile (`id="fleet-<slug>"`) | v1.9 |
| `/ui/routes` | The routes table (below) rendered as a small English page — the same data `GET /api/ui/routes` returns as JSON | v1.9 |
| `/ui/{client_slug}` | Client home: latest reports, pending actions, quick links. Stable section ids: `#stats`, `#pending`, `#features`, `#latest` | U1 |
| `/ui/{client_slug}/content` | This client's content library (videos, blogs, posts), plus what could be cloned from another client. `?q=` (case-insensitive search over the library) and `?kind=` (video\|blog\|social_post\|article\|image\|report\|note), both server-side | v1.9 |
| `/ui/{client_slug}/reports` | Reports hub (dated + `latest/`, HTML/PDF). `?date=YYYY-MM-DD` highlights and scrolls to that day's block (`id="d-<date>"`); `?open=latest` or `?open=<filename>` renders a prominent "Open" link to that file's `/files/` url at the top of the page — it never auto-redirects to the file | U1 |
| `/ui/{client_slug}/crm` | CRM kanban + contacts table (rows clickable; short id for nameless leads; phone/social columns + enriched / seed-unresolved badges). A "leads locked by plan" tile shows the current cap and locked count from `contact lock-status`, linking to the upgrade URL. A row past the plan's contact cap keeps its name but renders a lock badge in place of phone/social/identity columns and is not clickable through to the detail page. Params, applied server-side BEFORE pagination, in this order — `q` (case-insensitive substring over name, email, company and social/handle), `stage` (exact match on `lifecycle_stage`), `segment` (a saved CRM segment id, via `crm.go`'s `resolveSegment`), `locked` (`1` = locked-only, `0` = unlocked-only), `sort` (`created`\|`updated`\|`name`, a leading `-` for descending), `page` — with a locked contact staying exactly as redacted in a filtered view as it is unfiltered. An active filter shows a one-line "Showing N of M contacts — clear" banner linking to the unfiltered page. `?contact=<id>` 302s straight to `/ui/{client_slug}/contact/{id}`, through the same `store.resolve` the contact page itself uses, so a merged id's old link still lands on the survivor | U1 (read) / U3 (interact) |
| `/ui/{client_slug}/contact/{id}` | Full contact profile: all identities + seeds (with resolve state), the enrichment HOOKS timeline (the proof-of-life 'latest activities' that personalize email, each with evidence link + observed date + used-in), writing brief, and the activity timeline. A consolidated fragment's old id 302-redirects to its survivor (activities of merged fragments included); a `duplicate_suspects[]` record shows a resolve banner (merge vs unsuspect). For a locked contact this page instead renders the locked-contact view: name, lifecycle stage, first-seen date, source host, and a lock badge with an upgrade link — no identities, hooks, or activity timeline, and no email or DM action is rendered on the page | U3 |
| `/ui/{client_slug}/campaigns` | Campaign list: status pill (active/⏸ paused), goal type + objective, today's draft budget, pending-approval count, sent total + last-sent date; cards click through to the detail page. `?q=` (case-insensitive substring over the campaign slug) and `?status=active\|paused`, both server-side, compose (AND) | U3 |
| `/ui/{client_slug}/campaign/{campaign_slug}` | Campaign detail + operator edit form (`POST /api/ui/{c}/campaign-update`): prominent Pause/Resume, Goal section (goal_type select, objective, offer, value proposition, proof points one-per-line, CTA text), Companion-link section (plain-language instructions "how to get the link for each lead", on_fail select, default link), Sending section (daily draft budget). Same whitelist as `tool crm-store campaign update`; paused campaigns neither queue, draft, nor send (send blocker `campaign_paused` is transient — the draft stays approved and goes out on resume). `?tab=settings\|channel\|goal\|companion\|groups\|zillow` scrolls to that section on load (client-side; this page has no live-reload footer) | U3 |
| `/ui/{client_slug}/campaign/{campaign_slug}/harvest` | A harvest (Leads From Friends / Zillow) campaign's rows at one pipeline stage. `?stage=<id>` picks the stage (defaults `await`, or `kept` for Zillow) | U3 |
| `/ui/{client_slug}/sent` | Sent history: every sent email (newest first) joined read-only from `sent_log` + reply activities + pulled tracking events: recipient, campaign, step (cold/bump N), sendbox, sent time, and one status pill (replied > clicked > opened > sent). Stat tiles (total / replied / reply rate) + client-side filter, pre-selected on load from `?q=` (free text over each visible row) and `?status=replied`, and written back with `history.replaceState` on every change. Rows click through to the contact | T1 |
| `/ui/{client_slug}/approvals` | Interactive Approval Report (all campaigns; per-campaign at `/approvals/{campaign_slug}`). Per-draft approve/edit/hold/reject, PLUS a batch lane: every draft has a pre-checked checkbox, a sticky check-all/uncheck-all header, a "select high-confidence only" shortcut, and "Approve checked (N)" that approves exactly the ticked drafts (keeping any inline edits) — each decision still lands in `ui_inbox/approval_decisions.jsonl`. The channel tabs, confidence-band filter, campaign filter and free-text `?q=` are all pre-selected from the URL on load (`?tab=` is accepted as an alias for `?channel=`, which wins if both are given) and written back with `history.replaceState` on every change, so the current view is always the one the link points at | U2 |
| `/ui/{client_slug}/shortlist` | Discovery shortlist review (checkbox table). `?q=` (server-side, case-insensitive substring over the candidate's source name and url) | U2 |
| `/ui/{client_slug}/sendboxes` | Sendbox list + App Password connect form (`POST /api/ui/{c}/sendbox-auth`) | U2.5 |
| `/ui/{client_slug}/sources` | The places this client watches, and the search-term bank behind each one. `?src=<uid_hash>` opens one source's bank (server-side); `?q=` (v1.9, server-side, case-insensitive substring over the source's name and url) narrows the list | U1 |
| `/ui/{client_slug}/extension` | One-button extension install, English-only UI, multi-browser + multi-profile (OWNER DECISIONS 2026-09-10 items 1-3): on load, JS calls `GET /api/ui/{c}/browsers` and renders a browser `<select>` (auto-picked — the client's remembered choice, else the only detected browser, else the OS default, else the first "supported" one) plus, when the chosen browser reports 2+ Chrome profiles, a profile `<select>` (display name + email, remembered profile preselected); a browser with `support:"best_effort"` is labeled "(works, untested)"; zero detected browsers shows an English notice with a Google Chrome download link and disables Install. Clicking "Install extension" calls `POST /api/ui/{c}/install-extension` with `{browser, profile_directory}` from the pickers, which reveals the client folder highlighted inside extensions/ (macOS `open -R`, Windows `explorer /select`) AND opens that browser+profile's own extensions page in a new window brought to the front (OWNER finding 2026-09-10 item 2), in one call, returning `{folder, chrome_opened, ok, browser, profile_directory, extensions_url, error?, chrome_error?}` (`chrome_error` carries the OS-level error when the folder reveal succeeded but opening the extensions page failed; `chrome_opened` is the generic "did the extensions page open" flag regardless of which browser). The page then shows exactly two steps — turn on Developer mode (once), drag the highlighted folder onto "\<Browser\> extensions page[ of profile \"\<Name\>\"]" — plus the live check-in indicator ("✓ Connected" once the extension checks in) and a manual `<scheme>://extensions/` fallback line (Opera: `opera://extensions`, no trailing slash; everyone else: `<scheme>://extensions/`), muted and always shown once Install has been clicked regardless of `chrome_opened` ("If the extensions page did not come to the front, open it yourself" — never hidden on success, only `chrome_opened`'s own field semantics are unchanged). **Browser/profile detection (`ui_browsers.go`, OWNER DECISIONS 2026-09-10 item 2):** `detectBrowsers(goos, home)` is a pure function checking, per GOOS, whether each of six Chromium-based browsers' "Local State" JSON exists under `home` (Chrome/Edge = `support:"supported"`; Brave/Vivaldi/Chromium/Opera = `"best_effort"`; Safari/Firefox are not Chromium-based and are never listed) and, unless the browser is Opera ("has no profile directories" — no `--profile-directory` support, so its picker is never offered even when Local State lists more than one), parses `profile.info_cache` into `{dir, name, email}` rows. `GET /api/ui/{c}/browsers` returns `{browsers: [...], remembered: {browser, profile_directory} (from extension_registry.json), default_browser?}` — `default_browser` is a best-effort system-default guess (macOS `defaults read .../LSHandlers`, Windows `reg query .../UserChoice`, Linux `xdg-settings get default-web-browser`; any failure to detect omits the field, never errors). `POST /api/ui/{c}/install-extension` validates a given `browser` against the static catalog (unknown id → `400` with an English error) and, when profiles apply, builds the per-OS launch SEQUENCE from a static table (`buildExtensionsCommands`), always including `--new-window` on the first command (OWNER finding 2026-09-10 item 2a) so the extensions page lands in its own window instead of a background tab. macOS is TWO ordered commands (real-machine finding 2026-09-10: a single `open -na "<App>" --args --new-window [--profile-directory=<dir>] "<scheme>://extensions/"` reliably drops the url when the browser is already running, landing the new window on `chrome://newtab/` instead) — (1) `open -na "<App>" --args --new-window [--profile-directory=<dir>]` with no url opens the blank window, then, after `uiExtensionsWindowOpenDelay` (1.5s — 0.6s was still flaky in repeated live trials), (2) `open -a "<App>" "<scheme>://extensions/"` loads the extensions page into it, followed by a best-effort `osascript -e 'tell application "<App>" to activate'` — injectable as `uiActivateApp`, errors ignored — to bring that window to the front (`uiOpenExtensionsPageForBrowser` runs the whole sequence). Windows keeps the original single command, url included — `cmd /c start "" "<exe>" --new-window [--profile-directory=<dir>] <scheme>://extensions/` (`start` already focuses; not verified live, no Windows machine was available) — and so does Linux — `<binary> --new-window [--profile-directory=<dir>] <scheme>://extensions/` (focus step skipped — no `xdotool` assumed); if a running instance ever drops the url there too, the extensions page's always-visible manual fallback line covers it. Every command in the sequence runs through the same injectable `uiRunCommand`, so `go test` never launches a browser. It then persists `{browser, profile_directory}` into the client's `extension_registry.json` entry (`uiSaveExtensionRegistryBrowser`, same exclusive-flock read-modify-write as `source_registry.json`). Omitting the body keeps the pre-2026-09-10 behavior unchanged — Chrome, current window, nothing persisted. **Folder resolution (OWNER DECISIONS 2026-09-10 item A):** `uiExtensionInfo` picks the per-client folder in this order — an `extension_registry.json` pin, then `extensions/{client_slug}_extension/`, then, only for installs made before the 2026-09-10 rename, the legacy `extensions/{client_slug}/`; when none exist, `folder` in the response and the `folder_missing` note both name the current-convention `_extension` path, never the legacy one. **Help loop (OWNER DECISIONS 2026-09-10 item 4):** clicking Install also starts a client-side 90-second watch that polls this same page's server-rendered HTML every 10s (a `data-ext-checked-in="0"/"1"` marker on the card — extension check-ins are deliberately kept out of the SSE change feed, see `uiFingerprint`, so there is no separate status JSON call) and reloads the page once it flips to connected; if still not connected after 90s it reveals a diagnostic block (`#extdiag`, hidden by default) with the three common causes — Developer mode still off, the folder dropped into the wrong browser window/profile, the browser closed or the page navigated away — a "Reopen folder and browser" button that re-POSTs `install-extension` with the same browser/profile selection and restarts the watch, and a muted escape line pointing a company-managed-Chrome user at a personal machine/profile. The older `POST /api/ui/{c}/reveal-extension` (folder-only) endpoint still exists for compatibility. **(v1.9)** `?browser=<key>&profile=<dir>` pre-select the browser/profile `<select>`s on load (client-side JS reads `location.search`); the remembered `extension_registry.json` choice stays the default when either is absent | U2.7 |
| `/api/ui/{client_slug}/browsers` (`GET`) | Detected-browsers list for the extension page's install picker: `{browsers: [{id, name, path, scheme, support, profiles: [{dir, name, email}]}], remembered: {browser, profile_directory}, default_browser?}`. Read-only, so (unlike every other `/api/ui/...` route) it is a `GET`, not a `POST` — `handleUIAPI` special-cases it before the POST-only gate. See the `/ui/{client_slug}/extension` row above for the full contract | U2.7 |
| `/ui/assets/help/{filename}.mp4` | Short recorded install-walkthrough videos (Chrome, Edge), `//go:embed`ded into the binary (`ui_help_videos.go`) so no client install ships a source tree; served via `http.ServeContent` (Range/seek support, `Content-Type: video/mp4`, `Cache-Control: public, max-age=86400`, `X-Content-Type-Options: nosniff`, fixed deterministic mod-time) and gated by `b.uiAuth` the same as every other `/ui/...` route (host-header check in `--ui-auth host` mode; token cookie otherwise); unknown filenames 404. The `/ui/{client_slug}/extension` page embeds one of these right under the two install steps (not-connected state) or collapsed under a `<details>` "Watch the install video again" (connected state); client-side JS swaps `<video id="helpvideo">`'s `src` and the `#helpvideo-caption` text to match the chosen browser — Chrome and Edge get their own recording, every other Chromium browser reuses the Chrome one with a note that the steps are the same | v1.8 |
| `/files/...` | Raw file serving from the data root (reports, assets), read-only | U1 |
| `/events?scope=...` | SSE change feed (file-watch driven) | U1 |
| `/api/ui/...` | JSON read APIs + `ui_inbox` write endpoints (cookie-gated) | U1–U2 |
| `/api/ui/routes` (`GET`) | The routes table (`uiRoutes`, `ui_routes.go`) as JSON — the single source of truth this whole section describes, machine-readable: `[{path, params: [{name, desc}], shows}, ...]`. `uiAuth`-gated like every other `/api/ui` read; `/ui/routes` renders the same data for a human | v1.9 |
| `POST /api/ui/show` | `{"url":"/ui/leadup/crm?locked=1"}` → broadcasts SSE event `"show"` with that url to every connected `/events` client, which `location.assign()`s it (or offers a sticky "Open" link if the tab has unsaved input) — how an agent steers the operator's own already-open dashboard tab to the exact view it just described in chat. Same-origin-gated like every other `/api/ui` write; the url must be a relative `/ui/...` or `/files/...` path — no scheme, no `//host`, no `../` traversal, no encoded slash, no raw CR/LF — anything else is `400`. Answers `{"ok":true,"url":...,"subscribers":N}` | v1.9 |
| `tool ui show <path> [--bridge URL]` | CLI twin of `POST /api/ui/show` — a thin POST client, for a runtime with no browser tab of its own reacting to the SSE event but a human watching the dashboard a different way | v1.9 |
| `tool ui routes` | Prints the routes table as plain text (path, each param indented, then what the page shows) — no bridge needed, the table is compiled into the binary | v1.9 |

Rules: paths are stable once shipped (append, never repurpose). Agents hand the human absolute URLs. When the bridge is not running, agents fall back to today's chat/file-path flows and may offer the one-line command to start the bridge. **(v1.9, OWNER DECISION 2026-09-10)** Every filter/tab change on every page is reflected in the URL (`history.replaceState`) so the current view is always the one the link points at, and every new page MUST register in the `uiRoutes` table (`ui_routes.go`) — a test (`TestUIRoutesTableCoversRouterCases`) enforces this by diffing the router's cases against the table, so a page cannot ship without it. When an agent wants the human's own open dashboard tab to jump to a specific view rather than (or in addition to) handing over a link, `POST /api/ui/show` (or `tool ui show`) does that over the live SSE connection instead of a second link the human has to click.

## 6. File-bus contract

### 6.1 Read model (bridge → UI)

The bridge reads existing files as-is — no schema changes: `clients_index.md`, `outputs/**/report_state.json` + report HTML/PDF, `collector/jobs/*` + `collector/inbox/*`, `outreach/**/crm/*` (contacts/deals/tasks/pipelines via the same JSON the tools write), `sendboxes/sendboxes.json` (never `credentials.json`), `campaigns/*/campaign_config.json`, `outbox/*` (the Approvals page scans `campaigns/*/outbox/pending_approval/**/*.json` and shows only `status: pending_approval`), `approvals/approval_log.md`, `notifications/notification_log.md`, `automation/*`, and `history/discovery_shortlist.json` — the machine-readable shortlist mirror the CONTENT agent writes alongside the in-chat numbered list (playbook 02): `{"generated_at": "<ISO-8601 UTC>", "candidates": [{"n": <chat number>, "source_name", "source_url", "platform", "cadence_suggested": "daily|weekly|optional", "why", "classification"}]}`. The bridge never reads: `credentials.json`, `token.json`, `secrets/`, provider keys.

### 6.2 Write model (UI → agents): `ui_inbox/` only

The bridge writes ONLY append-only JSONL under dedicated `ui_inbox/` directories (single-line `O_APPEND` + fsync — safe because the ownership matrix makes the bridge the file's sole writer; v1.2 supersedes the earlier temp+rename wording, which cannot append):

- `clients/{c}/{bl}/outreach/ui_inbox/approval_decisions.jsonl` — `{ts, draft_id, decision: approve|reject|hold|edit, campaign?, edited_subject?, edited_body?, note?, ui_session}`. `edited_*` may accompany ANY decision (applied before it); `decision: edit` alone patches the draft and leaves it pending. POST `/api/ui/{client}/approval`.
- `clients/{c}/{bl}/ui_inbox/shortlist_decisions.jsonl` — `{ts, source_url, source_name?, decision: approve|skip, cadence?: daily|weekly|optional, ui_session}`. POST `/api/ui/{client}/shortlist` (body `{decisions: [...]}`, invalid entries skipped, response reports the queued count).
- (U3) `clients/{c}/{bl}/outreach/ui_inbox/crm_actions.jsonl` — task done / deal stage moves, each with evidence of the human click.

**The one sanctioned exception (v1.3, U2.5): sendbox credential entry.** `POST /api/ui/{client}/sendbox-auth` (`{slug, email, app_password}`) makes the bridge run the SAME auth code as `tool gmail auth` in-process: live SMTP+IMAP verification, then it writes `outreach/sendboxes/{slug}/credentials.json` (0600) and the `sendboxes.json` entry. This is deliberately NOT routed through `ui_inbox/` — agents read ui_inbox, and the App Password must never reach any agent-readable surface (chat, queue, log). Error responses are sanitized to a class name; the secret is never echoed. This is the only canonical write the UI can trigger outside `ui_inbox/`.

**The second sanctioned exception (v1.5, U3): operator campaign edits.** `POST /api/ui/{client}/campaign-update` (`{slug, patch}`) applies the SAME strict whitelist as `tool crm-store campaign update` (status active|paused, daily_quota 1..500, goal.{goal_type, objective, offer, value_proposition, proof_points, cta.text, companion_doc{instructions, on_fail, default_link}}; every other key is rejected loudly) and writes `campaign_config.json` atomically in-process. Rationale: campaign goal/budget/pause are OPERATOR-owned configuration — the human is the authority, so the edit takes effect instantly instead of waiting for an agent ingest cycle (pause must stop the very next send). The UI additionally appends an informational event to `outreach/ui_inbox/campaign_edits.jsonl` (`{ts, campaign, changed[], ui_session}`) so agents see that config shifted under them; it is a notification, not the write path. Everything else the UI does still goes through `ui_inbox/` only.

Consumption invariant: tools/agents merge `ui_inbox` into the canonical ledgers at the next run — `crm_store.py ingest-ui` (run at the start of every campaign daily run and again immediately before the send step) applies approval decisions with `by: ui` in `approvals/approval_log.md` (single-ledger invariant preserved; reject reasons feed `learning_log`), idempotent via the processed-line cursor `outreach/ui_inbox/.approval_cursor`; approved drafts are then honored by `gmail_client send` exactly like chat approvals. Shortlist decisions are consumed by the content agent (playbook 02, cursor `history/.shortlist_cursor`) and feed the same save path as chat-numbered approval. The bridge never writes canonical ledgers, CRM collections, configs, or profiles.

### 6.3 Ownership matrix

| Writer | May write |
|---|---|
| Agents + Python/Go tools | everything they write today (CRM via crm_store, drafts, reports, jobs, ledgers, configs) |
| Bridge | `ui_inbox/**`, `bridge/ui_token`, its existing collector inbox/status files, SSE (no disk) |
| Human (browser) | nothing directly — only via bridge `ui_inbox` endpoints |

One writer per file, append-only JSONL for decisions, atomic renames everywhere — the same discipline the storage adapter already enforces.

## 7. UI vs chat — division per surface

UI owns: approval review (cards, inline edit, approve/reject/hold, batch), shortlist review (checkboxes + evidence links), job queue/run monitor, CRM kanban/contacts, reports hub, dashboards (sendbox health, quota/warmup, metrics), Lead & Competitor tables, the clickable Feature Catalog home.

Chat owns: all natural-language declaration and correction (business context, goals, message-bank discussion), every conversational decision, tour-guide navigation ("mở [Approvals](.../ui/leadup/approvals), duyệt 3 draft đầu — hoặc trả lời `approve 1-3` ngay tại đây"), feature discovery, next-action guidance. The one-block Campaign Quick Start confirmation stays in chat (semi-structured, conversational).

Playbook rules to ship with U1/U2 (not before the surfaces exist): link-first handoffs with file-path fallback; an `[ACTION REQUIRED]` UI variant (link + what to click + chat fallback); the approval-gate amendment (chat OR ui_inbox, same trust); the agents-never-fetch rule; Stage-9 checkboxes.

## 8. Absorbing the Python tools into the bridge (G-phases)

Audit result (2026-07-19, full import/mechanism scan): 11 files, 7,739 lines, stdlib-only by design (the only non-stdlib imports are optional weasyprint/reportlab PDF fallbacks — dropped in the port). No eval/fork/signal/dynamic imports anywhere. Today the CRM layer refuses to run on Windows (`fcntl` hard requirement) — the Go port removes that blocker.

| Tier | Files | Port notes |
|---|---|---|
| G1 (with U1) | `email_verify` (Go `net.LookupMX` replaces dig/nslookup shell-outs); `storage/` read+write core (atomic write, ULID, cross-platform file lock — Flock/LockFileEx wrapper); unified report renderer (hand-rolled MD→HTML parser ports 1:1; PDF stays Chrome-headless via `os/exec`, wkhtmltopdf fallback; adopt the stricter scrub logic; merge the two ~51-line-divergent forks); unified `provider_openapi` (hand-rolled YAML line-scanner ports 1:1, no YAML lib; merge the 218-line-divergent copies, keep the outreach copy's guards + `notify` with `sendNotification` subject/message) | ~2,300 py-lines |
| G2 (with U2) | `crm_store` full port behind a CLI-compatible surface; its 3 `subprocess python3 report_renderer` call sites disappear (renderer is in-binary — also fixes the hardcoded `python3` name that breaks Windows); `import_leads` (zip+XML XLSX reader ports to `archive/zip`+`encoding/xml`; reimplement the small CSV delimiter sniffer) | ~2,750 |
| G3 | `gmail_client`: SMTP = implicit-TLS 465 via `tls.Dial` + `net/smtp` client (login + send only); IMAP = minimal client for the exact surface used — LOGIN, SELECT INBOX, UID SEARCH (ALL/range), UID FETCH RFC822, LOGOUT — no IDLE/APPEND/STORE; port the MIME build (CAN-SPAM footer, List-Unsubscribe) and the deterministic classifier with table tests; preserve the NFKC subject-gate normalization | ~800 |

CLI-compat contract: the binary exposes `solo-agency-bridge tool <name> <subcommand> [flags]` mirroring each Python CLI's flags and JSON stdout exactly. Validation gate per tool: a cross-validation harness runs Python and Go against the same fixture workspace and diffs JSON output (golden tests); a tool's Python original is retired only after parity is green plus one real daily run. During transition, playbooks say binary-first with `python3` fallback; after full parity, a sweep removes the Python paths and the user machine needs only the binary (existing dist pipeline: per-OS zips + SHA256SUMS + setup scripts).

## 9. Combined roadmap

| Phase | Ships | Risk |
|---|---|---|
| U0 | This contract | — |
| U1 + G1 | Read-only UI (home, jobs, status, reports, leads, CRM-read) + static serving + SSE + G1 tool ports + link-first playbook rules | near-zero (read-only) |
| U2 + G2 | Interactive approvals + shortlist via `ui_inbox`; crm_store/import_leads in Go; approval-gate amendment | medium — gated by cross-validation + ledger merge invariant. **U2 half shipped 2026-07-19** (UI pages + POST endpoints + Python `ingest-ui` + playbook wiring); G2 Go port still open and must carry `ingest-ui` over |
| U3 + G3 | CRM interactions, campaign controls (pause/quota), gmail port, Python retirement sweep | medium — IMAP/SMTP needs live validation on a real sendbox |

## 10. Risks and guardrails

- **Two writers**: prevented by the ownership matrix + append-only `ui_inbox` + atomic renames.
- **Collector protocol breakage**: extension routes/token untouched; UI code lives in new files; `main.go` changes limited to route mounting and subcommand dispatch. Coordinate with any concurrent collector work before editing shared files.
- **Scope creep**: vanilla JS embedded, no dependencies, each surface must keep a chat fallback; a surface that cannot state its chat fallback does not ship.
- **Approval integrity**: nothing in the UI creates an auto-send path; `manual_all` semantics unchanged; every ui_inbox decision carries `ui_session` provenance and lands in the canonical ledger before any send.
- **Windows**: G1's lock wrapper + in-binary renderer are the unlock; add Windows Chrome path candidates (`Program Files` locations) to the PDF exec candidates during the port.

## 11. Non-goals

No cloud/hosted UI, no auth beyond loopback Host-header validation (or, opt-in, the legacy localhost token — §4), no mobile app (responsive pages suffice), no UI-side LLM calls, no bypassing of approval gates, and no second process.
